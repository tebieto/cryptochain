{
  "name": "cryptochain",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
  "coverage": "jest --coverage && cat ./coverage/lcov.info | coveralls",
  "test": "jest --watchAll",
  "test_2": "jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "coveralls": "^3.0.9",
    "jest": "^23.6.0"
  },
  "jest": {
    "collectCoverageFrom": [
        "**/*.js",
        "!data/keyMap.js",
        "!/node_modules/"
    ]
  }
}
